import { redirect } from "next/navigation";

/**
 * /my-profile ถูกยุบไปรวมกับ /student แล้ว
 *
 * เหลือไว้เป็นทางผ่านไม่ใช่ลบทิ้ง เพราะลิงก์เดิมถูกส่งต่อกันไปแล้วทั้งใน LINE
 * ในบุ๊กมาร์กของนักเรียน และในหน้าเก่าที่ยังไม่ได้ไล่แก้ทุกจุด — 404 ตรงนี้
 * แปลว่าคนที่เคยใช้ทางเดิมเจอทางตัน โดยที่หน้าปลายทางยังอยู่ดี ๆ
 *
 * redirect ถาวร (308) เพื่อให้เบราว์เซอร์กับเครื่องมือค้นหาอัปเดตที่อยู่ให้เอง
 */
export default function MyProfileRedirect(): never {
  redirect("/student");
}
